/// <reference path="../node_modules/typescript-atomizer-typings/atom" />

/**
 * Represents the main entry point into the TypeScript Atomizer plugin.
 */
var typescriptAtomizerPlugin: AtomPlugin = {
    /**
     * Called when Atom activates the plugin.
     */
    activate: () => {

    },

    /**
     * Called when Atom deactivates the plugin.
     */
    deactivate: () => {

    }
}

export = typescriptAtomizerPlugin;
